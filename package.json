{
  "name": "steerhook",
  "private": true,
  "type": "module",
  "description": "Property tests and acceptance scenarios for the steerhook plugin. The plugin itself has no dependencies.",
  "scripts": {
    "check": "nuka check",
    "test": "node --test \"test/*.test.ts\"",
    "acceptance": "nuka run features"
  },
  "devDependencies": {
    "@hegeldev/hegel": "0.4.5",
    "nukadoko": "0.12.0"
  }
}
