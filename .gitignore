# Task specs and reports handed to delegated agents. They carry the context a
# piece of work was decided in, which includes private feedback.
.claude-team/
CLAUDE.local.md

# Node tooling for the acceptance scenarios (nukadoko). The plugin runs without it.
node_modules/
.nukadoko/
.env
.env.*
!.env.example

# Hegel's failing-example database for the property tests.
.hegel/
